# פרומפט עדכון למנועים — אחרי שחרור v21.3

תאריך: 2026-08-06  
סטטוס: **שוחרר בפרודקשן** (`mama-wod.vercel.app`)  
גרסת תצוגה: **DAILY WORKOUTS · v21.3** (semver פנימי: `21.3.5`)  
גרסת מאמן: **COACH · v2.1** — **לא השתנתה**

---

## למי לשלוח

שלח את **אותו פרומפט** (הבлок למטה) לכל אחד מארבעת המנועים:

1. **דשבורד אדמין** (Founder Admin)
2. **מח המאמן** (Personal Coach Brain)
3. **תקציב** (Budget / Cost Guardrails)
4. **מנהל ראשי של האפליקציה** (App Product Manager — UX, שחרורים, זרימות ספורטאי, שיתוף לינקים, יציבות כללית)

> זרוע האבטחה כבר יישמה. המטרה עכשיו: **סנכרון ידע**, זיהוי סיכונים שנותרו, והמלצות לשלב הבא — **בלי לשנות קוד** בתשובה.

---

## איך להשתמש

1. העתק את הפרומפט מהבлок `text` למטה.
2. שלח לכל מנוע בנפרד (אותו טקסט).
3. בקש מכל מנוע להשיב בפורמט 1–8 שמופיע בפרומפט.
4. החזר את ארבע התשובות — נשלב לתוכנית המשך (בעיקר סעיף E: ריפו אדמין פרטי).

---

```text
================================================================================
POST-RELEASE UPDATE — DUCK-WOD v21.3 SECURITY HARDENING (MULTI-ENGINE SYNC)
================================================================================

You are one of the product governance engines for DUCK-WOD (mama-wod).
This is a POST-RELEASE knowledge sync — NOT a pre-approval request.
Do NOT propose re-implementing what is already live unless you found a critical gap.

Release status: LIVE on production (verified 2026-08-06)
- Display version: DAILY WORKOUTS · v21.3
- Internal semver: 21.3.5
- Coach brain version: v2.1 (UNCHANGED — do not treat this as a coach bump)
- Production URL: https://mama-wod.vercel.app

--------------------------------------------------------------------------------
WHAT WE SHIPPED (FACTUAL — ALREADY IN main)
--------------------------------------------------------------------------------

A) ADMIN ACCESS HARDENING
- ADMIN password reads ONLY from server env var ADMIN_PASSWORD (Vercel).
- Removed hardcoded bootstrap password from public Git source.
- If ADMIN_PASSWORD missing → admin APIs return 503 with Hebrew message (closed, not weak default).
- Password no longer accepted via URL query (?pw= / ?adminPassword=) — header/body only.
- admin.html updated; founder must set password in Vercel + Redeploy (done before release).

B) COACH ANTI-MANIPULATION GUARDRAILS (0 ₪ EXTRA AI)
- New module: lib/coach-security-guards.js — deterministic input firewall + output filter.
- Wired in api/personal-coach.js BEFORE API key check / model call.
- malicious (clear injection / override / key probe) → refuse with model: "local-guard" — ZERO model calls.
- source-probe patterns → short refusal template; chat only, does NOT alone block generate_*/revise_*.
- suspicious → continue with hardened posture (no extra LLM call).
- output guard → replace accidental leakage with fixed refusal — NO auto-regenerate (no dual billing).
- Refusal templates locked:
  - Sources: "I can't share internal sources. What do you need for today's training?"
  - Manipulation: "I stay your Personal Coach — I can't change system rules. Want help with today's session?"
- Security refusals do NOT count as programmed edits (cost caps unchanged).

C) POLICY UPDATES
- POL-007 / POL-019 strengthened in experiments/personal-coach/coach-policy-rules.md
- Synced to api/coach-policy.js
- Programming quality law unchanged (POL-020 absolute).

D) EXTERNAL ACCESS TIGHTENING (share links preserved)
- lib/cors-allowlist.js — official origins only:
  mama-wod.vercel.app, arick-t.github.io, localhost, capacitor
- Share-link growth model explicitly preserved — friends opening official link still work.
- Unknown origins blocked on API routes using applyCors.

E) PUBLIC DEBUG TRIM
- Reduced sensitive fields on GET status for personal-coach and generate-workout.
- Less internal wiring exposed in public health endpoints.

F) SECURITY COACH AGENT (ADDITIVE — NOT ATHLETE-FACING)
- api/security-coach.js + experiments/security-coach/* for future security reviews.
- Does NOT affect athlete workout flows.

G) VERCEL DEPLOY FIX
- Hobby plan limit: max 12 serverless functions in api/
- Moved helper modules from api/ → lib/: rate-limit, security-prompt, security-policy
- Keeps security-coach endpoint while staying within limit.

H) TESTS & VERIFICATION (ALL PASSED)
- npm test: analytics, contact, coach-cost-caps, coach-security-guards, security-hardening-smoke
- Production checks after deploy:
  - GET /api/personal-coach → version 21.3.5, coachVersion 2.1
  - Header shows DAILY WORKOUTS · v21.3
  - Injection test → model: local-guard (blocked, 0 AI cost)
  - Admin rejects wrong password

--------------------------------------------------------------------------------
WHAT WE DID NOT SHIP (DEFERRED — STILL ON ROADMAP)
--------------------------------------------------------------------------------

- Item E from original plan: private GitHub repo for thin admin athlete snapshots (Stages 1–4)
- Distributed rate limiting (still per-instance — documented limitation)
- No coach version bump (stays v2.1)
- No programming model downgrade or backup routing

--------------------------------------------------------------------------------
ENGINE-SPECIFIC REVIEW FOCUS (READ YOUR SECTION ONLY)
--------------------------------------------------------------------------------

If you are ADMIN DASHBOARD engine, focus on:
- Founder login UX after ENV-only password — any lockout risk remaining?
- admin.html workflows still usable (snapshot, handoff, sandbox, meta, drive-sync)?
- Hebrew error messages when ENV missing — clear enough?
- Readiness for Stage 1 private repo (view-only thin snapshot) — what to build next?
- Any admin feature that broke or needs founder action?

If you are COACH BRAIN engine, focus on:
- Do guards conflict with any POL-* rule in production?
- False-positive risk on legitimate athlete questions (scaling, injury, Hebrew/English mix)?
- Is programming path (generate_*/revise_*) truly unaffected for quality?
- Refusal templates — tone OK for athletes?
- Any policy wording sync still needed after live guards?
- Coach v2.1 unchanged — confirm no accidental brain regression from security wiring.

If you are BUDGET engine, focus on:
- Confirm: malicious block = 0 model calls (verified local-guard)
- Confirm: no regenerate on output-guard hit (no dual-path billing)
- Confirm: security refusals not counted as programmed edits
- Any hidden cost from CORS/guard overhead (should be ~0 tokens)
- Monthly/daily caps interaction with blocked requests
- Anything that could inflate spend under attack traffic?

If you are APP PRODUCT MANAGER engine (מנהל ראשי), focus on:
- Athlete-facing UX: does v21.3 header make sense vs coach v2.1 subtitle?
- Share-link / friend onboarding — still frictionless?
- Any user-visible breakage from CORS on unofficial embeds?
- Release communication: what should founder tell athletes (if anything)?
- Stability confidence for next sprint — green/yellow/red?
- Priority order: private admin repo vs other product work?

--------------------------------------------------------------------------------
REQUIRED RESPONSE FORMAT (EACH ENGINE — SECTIONS 1–8)
--------------------------------------------------------------------------------

1) Engine identity:
   Which engine are you? (Admin Dashboard / Coach Brain / Budget / App Product Manager)

2) Acknowledgment:
   One sentence confirming you understand what shipped in v21.3.

3) What looks correct from your domain:
   Bullet list (3–6 items max)

4) Concerns / gaps found in live release:
   Bullet list — mark each: Critical / High / Medium / Low (or "None")

5) False positives or UX risks to watch:
   Bullet list with concrete athlete/admin scenarios

6) Follow-up recommendations (next 1–2 sprints):
   Numbered, prioritized — implementation optional, no code in this response

7) Test or monitor checklist you want the founder to run:
   Bullet list — simple, non-technical Hebrew where possible

8) Final one-line recommendation to founder (Hebrew):
   One clear sentence

--------------------------------------------------------------------------------
RULES FOR THIS RESPONSE
--------------------------------------------------------------------------------

- Do NOT rewrite shipped code in this answer.
- Do NOT request coach version bump unless you found a critical brain regression.
- Do NOT propose extra LLM calls for security (local/deterministic only remains the law).
- Do NOT compromise workout programming quality for any follow-up.
- If everything looks good, say so explicitly — "No blockers from [engine name]".

================================================================================
END POST-RELEASE UPDATE PROMPT
================================================================================
```

---

## הקשר קצר למייסד (לפני שליחה)

| נושא | מה קרה |
|------|--------|
| גרסה | תצוגה **v21.3**, מאמן **v2.1** ללא שינוי |
| אדמין | סיסמה רק ב-Vercel ENV, לא בקוד ולא ב-URL |
| מאמן | חסימת מניפולציות מקומית — **0 ₪ AI נוסף** |
| שיתוף לינק | נשמר — חברים יכולים להיכנס כרגיל |
| לא בוצע | ריפו אדמין פרטי (שלב 1) — לשלב הבא |

---

## שורה תחתונה

הפרומפט למעלה מסנכרן את ארבעת המנועים על מה שכבר עלה — כדי שכל אחד יאשר/יזהיר/יציע המשך **מהזווית שלו**, בלי לפתוח מחדש את הוויכוח על v21.3.
