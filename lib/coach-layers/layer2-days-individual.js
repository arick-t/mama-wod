/**
 * How many days ONE athlete is programmed for. Individual path only.
 *
 * The counterpart to layer2-days-studio.js, split out of layer2-general for the same reason on
 * 2026-09-03. A studio's seven-day default has no meaning here: an individual's own training days
 * ARE the weekly structure, which is the distinction the competitor layer had to state separately
 * before this split existed.
 */
module.exports =
  "--- HOW MANY DAYS (one athlete) ---\n" +
  "Training days and rest days come from the intake exactly. Never invent one, never move one.\n" +
  "Their marked days ARE the weekly structure — there is no default to fall back on.\n";
